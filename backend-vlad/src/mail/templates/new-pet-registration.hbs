<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Nueva Mascota Registrada</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .content {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
        .info-item {
            margin-bottom: 10px;
        }
        .label {
            font-weight: bold;
            color: #666;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 0.9em;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>¡Nueva Mascota Registrada en PeludosClick!</h1>
    </div>
    
    <div class="content">
        <div class="info-item">
            <span class="label">Nombre de la Mascota:</span>
            <span>{{petName}}</span>
        </div>
        
        <div class="info-item">
            <span class="label">Email del Dueño:</span>
            <span>{{ownerEmail}}</span>
        </div>
        
        <div class="info-item">
            <span class="label">Número de Medalla:</span>
            <span>{{medalString}}</span>
        </div>
        
        <div class="info-item">
            <span class="label">Teléfono:</span>
            <span>{{phoneNumber}}</span>
        </div>
        
        <div class="info-item">
            <span class="label">Descripción:</span>
            <p>{{description}}</p>
        </div>
        
        <div class="info-item">
            <span class="label">Fecha de Registro:</span>
            <span>{{registrationDate}}</span>
        </div>
        
        {{#if image}}
        <div class="info-item">
            <span class="label">Foto:</span>
            <p>Adjunta en el email</p>
        </div>
        {{/if}}
    </div>
    
    <div class="footer">
        <p>Este es un mensaje automático del sistema de PeludosClick</p>
    </div>
</body>
</html> 